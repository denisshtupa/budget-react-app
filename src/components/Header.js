import React from 'react'

// THIS IS A FUNCTIONAL COMPONENT ( no need to use state, so we use Functional Component instead of cClass Component)
export const Header = () => {
    return (
        <h2>
            Expense tracker
        </h2>
    )
}
